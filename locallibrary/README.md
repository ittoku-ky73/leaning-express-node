# Express Local Library example

このアプリは、Node/Expressを使用した地域図書館のウェブサイトのサンプルです。データベースはPostgresqlを使用しています。本番環境は、Herokuです。

**セットアップ**

1. `docker-compose build`

2. `docker-compose up`

3. open http://localhost:3000
